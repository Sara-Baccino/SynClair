Sto implementando synclair-matching all'interno del monorepo SynClair.
synclair-core e synclair-structure sono già stati implementati parzialmente/completamente.
Devi lavorare sul repository esistente e NON ricreare una nuova architettura parallela.

OBIETTIVO GENERALE: Implementare synclair-matching come modulo indipendente e riutilizzabile
per population matching.

Il modulo deve essere:
- indipendente dalla GUI;
- indipendente da FastAPI;
- indipendente da synclair-reporting;
- dipendente da synclair-core;
- compatibile con l'astrazione AnalysisModule di synclair-core;
- configurabile tramite Pydantic;
- privo di variabili globali;
- testabile singolarmente;
- estendibile con nuovi algoritmi;
- compatibile con i risultati prodotti dagli altri moduli SynClair.

NON deve essere progettato come semplice wrapper del vecchio codice.
Il codice legacy allegato è una REFERENCE IMPLEMENTATION scientifica.
Devi recuperare le formule, le strategie e la logica statistica dove possibile,
ma rifattorizzarle secondo l'architettura SynClair.
Non modificare arbitrariamente formule o criteri statistici.
Se trovi una possibile correzione o modifica metodologica, segnalala prima.
============================================================
PRIMA DI SCRIVERE CODICE Analizza il repository esistente.
In particolare verifica:

1. synclair-core
   - DataConfig
   - dataset loader
   - preprocessing
   - imputation
   - scaling
   - transformers
   - AnalysisResult
   - BaseModule / AnalysisModule
   - ExecutionContext
   - serialization
   - eventuali artifact/result model

2. synclair-structure
   - StructureModule
   - StructureModuleConfig
   - configurazioni Pydantic
   - uso di AnalysisResult
   - convenzioni architetturali
   - pipeline
   - gestione degli errori

3. synclair-matching attuale.

NON assumere API.
Leggi il codice realmente presente nel repository.
Prima di implementare crea una breve tabella:
LEGACY FUNCTION
→ NUOVO FILE
→ COMPONENTE
→ RIUTILIZZO / RIFATTORIZZAZIONE
→ NOTE

============================================================
PRINCIPIO ARCHITETTURALE FONDAMENTALE: SynClair deve permettere di comporre più analisi.
La comunicazione tra moduli deve avvenire attraverso gli oggetti/resultati
comuni definiti da synclair-core.

Usa AnalysisResult di synclair-core come contratto comune.
Non creare un secondo sistema parallelo di risultati.

Il risultato di MatchingModule deve poter contenere, secondo le strutture
già disponibili nel core:

- dataset/input reference;
- dataset filtrato;
- dataset dopo trimming;
- matched treated;
- matched control;
- unmatched treated;
- unmatched control;
- matching identifiers;
- propensity scores;
- distances;
- matching metadata;
- balance before;
- balance after;
- overlap diagnostics;
- outlier diagnostics;
- imbalance diagnostics;
- configuration utilizzata;
- preprocessing applicato;
- eventuali plot/artifact;
- informazioni necessarie per la serializzazione.

Se AnalysisResult attualmente non supporta adeguatamente questi elementi, NON modificarlo immediatamente.

Prima segnala l'incompatibilità e proponi la modifica minima necessariaal core.

Dal codice allegato recupera e rifattorizza almeno:

compute_ps()
compute_smd_full()
check_overlap()
identify_complete_variables()
match_mahalanobis_ps()

Se presenti nel materiale allegato, recupera anche:

- optimal matching;
- Hungarian / linear_sum_assignment;
- caliper;
- distance calculations;
- categorical balance;
- statistical tests;
- post-matching diagnostics.

NON mantenere:

- COVARIATES globali;
- TREATMENT_COL globale;
- PS_WEIGHT globale;
- CALIPER_WIDTH globale;
- PCT_MAHA globale;
- print() come reporting;
- path hardcoded;
- esportazione Excel/PDF;
- configurazioni globali;
- stato globale.

Usa questa struttura come base:

packages/synclair-matching/

├── pyproject.toml
├── README.md
├── src/
│   └── synclair_matching/
│
│       ├── __init__.py
│
│       ├── preprocessing/
│       │   ├── __init__.py
│       │   ├── filters.py
│       │   ├── missing.py
│       │   └── propensity.py
│
│       ├── representation/
│       │   ├── __init__.py
│       │   ├── propensity_score.py
│       │   └── covariate_space.py
│
│       ├── distance/
│       │   ├── __init__.py
│       │   ├── base.py
│       │   ├── euclidean.py
│       │   └── mahalanobis.py
│
│       ├── matching/
│       │   ├── __init__.py
│       │   ├── base.py
│       │   ├── nearest_neighbor.py
│       │   ├── propensity_matcher.py
│       │   ├── optimal_matcher.py
│       │   └── cluster_matcher.py
│
│       ├── metrics/
│       │   ├── __init__.py
│       │   ├── balance.py
│       │   ├── overlap.py
│       │   └── distances.py
│
│       ├── diagnostics/
│       │   ├── __init__.py
│       │   ├── balance.py
│       │   ├── unmatched.py
│       │   ├── outliers.py
│       │   └── imbalance.py
│
│       ├── sensitivity/
│       │   ├── __init__.py
│       │   └── base.py
│
│       ├── visualization/
│       │   ├── __init__.py
│       │   └── plots.py
│
│       ├── config/
│       │   ├── __init__.py
│       │   ├── propensity_config.py
│       │   ├── overlap_config.py
│       │   ├── distance_config.py
│       │   ├── matching_config.py
│       │   └── matching_module_config.py
│
│       ├── pipeline/
│       │   ├── __init__.py
│       │   └── matching_module.py
│
│       └── exceptions.py
│
└── tests/
    ├── test_propensity.py
    ├── test_balance.py
    ├── test_overlap.py
    ├── test_distance.py
    ├── test_matching.py
    └── test_matching_integration.py


NON creare necessariamente tutti questi file subito.
Crea solo quelli necessari alla fase corrente.

Il sistema deve distinguere chiaramente:

1. preprocessing
2. representation
3. distance
4. matching strategy
5. diagnostics
6. metrics
7. visualization
8. pipeline orchestration

Non legare rigidamente:
Propensity Score → un solo matcher.

Deve essere possibile concettualmente avere:

representation = propensity_score
distance = logit_ps
matching = nearest_neighbor

oppure:

representation = covariate_space
distance = mahalanobis
matching = nearest_neighbor

oppure:

representation = covariate_space
distance = mahalanobis
matching = optimal

oppure combinazioni supportate dall'implementazione.

Non implementare combinazioni che non hanno una base algoritmica
nel codice legacy senza prima segnalarlo.

Tutte le scelte metodologiche devono essere configurabili.
Creare almeno:

PropensityConfig
OverlapConfig
DistanceConfig
MatchingConfig
MatchingModuleConfig

MatchingModuleConfig deve poter descrivere:

- treatment_col
- covariates
- id_col
- population/filtering
- exact matching constraints
- preprocessing requirements
- propensity model
- representation
- distance
- matching strategy
- ratio
- replacement
- caliper
- neighborhood parameters
- optimization parameters
- balance metrics
- overlap diagnostics
- outlier detection
- imbalance detection
- sensitivity analysis

Il dataset NON deve essere memorizzato nel config.
Usare il preprocessing di synclair-core quando possibile. Non duplicare: imputation, scaling, encoding, trasformazioni generiche.

synclair-matching può avere preprocessing specifico del problema, ad esempio: filtering; trimming; preparazione delle variabili di matching; gestione specifica della propensity model.
Ogni trasformazione applicata deve essere registrata nel risultato.
Il sistema NON deve modificare silenziosamente le scelte dell'utente.


============================================================
OVERLAP - Separare:

A. overlap analysis
B. trimming

Non creare una funzione che analizza e modifica contemporaneamente il dataset.

Il risultato dell'analisi deve essere strutturato.

Il trimming deve essere un'operazione esplicita e configurabile.

Supportare almeno:

- common support;
- percentile trimming.

Preparare l'architettura per ulteriori filtri.

============================================================
BALANCE: rifattorizzare compute_smd_full(). Supportare:

- SMD non pesato;
- SMD pesato;
- absolute SMD;
- treated mean;
- control mean;
- pooled SD.

Preparare l'API per: KS, Jensen-Shannon;variance ratio; Chi-square; Fisher.

Il confronto deve essere disponibile: BEFORE MATCHING vs AFTER MATCHING

DISTANCE: Implementare inizialmente:

- Euclidean;
- Mahalanobis.

Creare un'interfaccia comune.

Preparare l'estensione per:

- Gower;
- ordinal distance.

Non implementare algoritmi privi di una reference implementation
sufficiente.

============================================================
MATCHING: Creare una classe base:

MatchingAlgorithm

con interfaccia concettuale:

fit(...)
match(...)
get_result(...)

Implementare inizialmente:

1. Nearest Neighbor 1:1
2. Propensity Score Matching
3. Mahalanobis + Propensity Score
4. Optimal/Hungarian SOLO se il codice legacy allegato contiene
   una implementazione concreta sufficiente.

Supportare progressivamente:

- 1:1;
- 1:K;
- replacement;
- caliper.

Non implementare subito tutte le combinazioni.

============================================================
DIAGNOSTICS: Il modulo matching deve produrre diagnostica direttamente collegata
alla qualità del matching.

Almeno:

- matched/unmatched units;
- matching rate;
- distance distribution;
- balance before/after;
- covariate ancora sbilanciate;
- common support;
- trimming;
- outlier;
- elementi non matchati.

Le diagnostiche devono essere dati strutturati, non print().

============================================================
VISUALIZATION - Per ora solo funzioni pure.

Implementare progressivamente:

- Love plot;
- propensity score distribution;
- overlap plot;
- distance distribution.

NON generare: PDF; Word; HTML; Excel. Questo appartiene a synclair-reporting.

============================================================
CROSS-MODULE COMPATIBILITY: questo punto è fondamentale. Un modulo NON deve dipendere direttamente da un altro modulo.

Quindi progetta AnalysisResult e i metadata in modo che validation
possa ricevere:

- dataset iniziale;
- dataset filtrato;
- dataset matched;
- treatment/control;
- covariates;
- matching weights;
- propensity scores;
- match identifiers;
- configuration;
- diagnostics.

Analogamente, un futuro synclair-survival deve poter consumare un dataset/resultato prodotto da matching senza conoscere
l'implementazione interna di matching.

NON implementare synclair-validation o synclair-survival adesso.

Devi soltanto assicurarti che il contratto dei risultati sia sufficiente.

============================================================
COMPOSABILITÀ: SynClair dovrà permettere in futuro una sequenza come:

dataset
→ cleaning
→ matching
→ validation

I moduli devono quindi poter ricevere: un dataset iniziale oppure un dataset derivato; oppure dati/materialized results provenienti da AnalysisResult.

Non creare ancora un workflow engine globale.

La composizione verrà gestita successivamente dal layer applicativo/core.

============================================================
REPORTING - NON importare synclair-reporting. NON creare export PDF/Excel/Word dentro matching.

Matching deve produrre dati e artifact strutturati.

In futuro synclair-reporting potrà ricevere i risultati di uno o più moduli
e generare: PDF; Excel; HTML; Word.

Il modulo matching deve quindi conservare i dati necessari a tale
esportazione senza occuparsi direttamente del formato finale.

============================================================
BACKEND - Non implementare FastAPI adesso.

In futuro voglio poter avere un backend/main capace di eseguire:

core
structure
matching
validation
survival
ecc.

ma il backend non deve contenere la logica scientifica. La logica deve rimanere nei rispettivi package.

============================================================
VERTICAL SLICE
============================================================

NON implementare subito l'intero framework.

Dopo aver analizzato il repository, procedi con questa vertical slice:

DataConfig
    ↓
core preprocessing
    ↓
MatchingModuleConfig
    ↓
MatchingModule
    ↓
propensity score
    ↓
common support
    ↓
1:1 nearest-neighbor matching
    ↓
SMD before/after
    ↓
unmatched diagnostics
    ↓
AnalysisResult

Usa un dataset sintetico deterministico piccolo.

Il test deve essere realmente eseguibile nel monorepo.

Deve verificare almeno:

1. import di synclair-core;
2. import di synclair-matching;
3. DataConfig;
4. preprocessing;
5. propensity score;
6. overlap;
7. matching;
8. labels/identifiers matched;
9. SMD before/after;
10. unmatched units;
11. AnalysisResult;
12. serializzazione tramite synclair-core.

============================================================
REGOLE DI IMPLEMENTAZIONE:
NON:
- riscrivere synclair-core senza motivo;
- introdurre plugin globali;
- introdurre GUI;
- introdurre FastAPI;
- introdurre reporting;
- creare dipendenze circolari;
- duplicare il preprocessing del core;
- usare variabili globali;
- usare print() per reporting;
- inventare algoritmi non presenti nella reference implementation.

Quando trovi una incompatibilità:

1. fermati;
2. descrivi il problema;
3. indica quale API è incompatibile;
4. proponi la modifica minima;
5. modifica il codice solo dopo aver verificato che sia necessaria.

============================================================
ORDINE DI LAVORO
FASE 1: Analisi repository + codice legacy.

Output:

- tabella mapping legacy → nuovo codice;
- dipendenze rilevate;
- incompatibilità;
- proposta architetturale;
- lista dei file da creare/modificare.

NON scrivere ancora tutta l'implementazione.

FASE 2: Implementare vertical slice:

- propensity;
- overlap;
- balance;
- distance;
- nearest neighbor;
- MatchingModule;
- config;
- AnalysisResult integration;
- integration test.

FASE 3: Solo dopo che la vertical slice passa:

- 1:K;
- replacement;
- caliper avanzati;
- Mahalanobis + PS;
- optimal/Hungarian se supportato dal legacy;
- ulteriori distance;
- ulteriori diagnostics.

FASE 4: Solo successivamente:

- sensitivity;
- ulteriori balance metrics;
- ulteriori filtering;
- ulteriori strategie.

============================================================
OUTPUT OBBLIGATORIO DOPO OGNI FASE: Mostrami sempre:

1. file creati;
2. file modificati;
3. funzioni legacy recuperate;
4. funzioni legacy rifattorizzate;
5. logica scientifica preservata;
6. decisioni architetturali;
7. dipendenze introdotte;
8. test eseguiti;
9. comando esatto per eseguirli;
10. risultato dei test;
11. problemi residui;
12. eventuali modifiche richieste a synclair-core.

NON passare automaticamente alla fase successiva.

Dopo ogni fase fermati e attendi la mia approvazione.

============================================================
PRIMO TASK
============================================================

Per ora esegui SOLTANTO FASE 1.

Non creare ancora l'intero modulo.

Analizza:

- repository;
- synclair-core;
- synclair-structure;
- codice legacy allegato.

Produci:

A. mapping legacy → SynClair;
B. struttura proposta definitiva;
C. dipendenze tra package;
D. contratto previsto di MatchingModule;
E. contratto previsto di MatchingAlgorithm;
F. struttura di MatchingModuleConfig;
G. struttura dei risultati necessari per la composizione tra moduli;
H. eventuali incompatibilità con core/structure;
I. elenco delle modifiche minime necessarie.

Poi fermati.