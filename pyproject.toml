[project]
name = "synclair"
version = "0.1.0"
requires-python = ">=3.12"

[dependency-groups]

dev = [
    "pytest",
    "ruff",
    "mypy",
    "pre-commit",
]

[tool.uv.workspace]
members = ["packages/*"]
