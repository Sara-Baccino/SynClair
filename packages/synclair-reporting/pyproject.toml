[project]
name = "synclair-reporting"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sara Baccino", email = "baccinosara.2000@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "lxml>=6.1.2",
    "reportlab>=5.0.1",
]

[project.scripts]
synclair-reporting = "synclair_reporting:main"

[build-system]
requires = ["uv_build>=0.9.13,<0.10.0"]
build-backend = "uv_build"
